version: '2'
services:
  usquestionnaire-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/usquestionnaire/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=usquestionnaire
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
